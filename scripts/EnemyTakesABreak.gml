whatIWant = whatDoIWantToDo();

if (whatIWant=="Fooooodddd" && stopAtEvent!=1){
    EnemyGoesTo(obj_fridge);
    }
    
if (whatIWant=="Gonna whiz" && stopAtEvent!=1){
    //states=states.needsToilet;
    EnemyGoesTo(obj_toilet);
    }
    
if (whatIWant=="Yawn" && stopAtEvent!=1){
    EnemyGoesTo(obj_bed);
   // states=states.tired;
    }
    
    if (whatIWant=="Dum de dum" && stopAtEvent!=1){
      //  states=states.bored; currentBroke!!!
    }
